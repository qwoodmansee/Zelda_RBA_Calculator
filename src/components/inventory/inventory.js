import React from 'react'
import PropTypes from 'prop-types';

export default class Inventory extends React.Component {
    render () {
        return (
            <div>Inventory Example</div>
        )
    }
}